# DECISIONS.md — Stitches decisions log

One line per decision: date · what · why. Append whenever a session deviates from
SPEC/DESIGN (per the PLAN.md session ritual).

- 2026-07-10 · Docs renamed to `docs/SPEC.md` / `DESIGN.md` / `PLAN.md` / `handoff.md` · match PLAN.md's binding references.
- 2026-07-10 · Node pinned 24.18.0 (nvm + `.nvmrc` + `engines`); dev machine had 26.0.0 · SPEC §5 says Node 24 LTS on dev and VPS.
- 2026-07-10 · **Version policy: pin to AI-known majors.** Scaffolded with `create-vite@7.1.3` → Vite 7.3.6, not current Vite 8 · AI assistance breaks on post-knowledge majors; deviates from SPEC §5 "current major" but stays inside its "7/8-era" note.
- 2026-07-10 · `@vitejs/plugin-react` held at 5.2.0 · 6.x peer-requires Vite ^8 only — never bump without deliberately moving to Vite 8.
- 2026-07-10 · TypeScript 5.9.3 (template shipped ~5.8.3; registry latest is 7.0.2) · SPEC pins 5.x; TS 7 (2-day-old Go rewrite) is peer-rejected by typescript-eslint (`<6.1.0`).
- 2026-07-10 · React Router 7.18.1, package `react-router` only · SPEC pins ^7; v8.2.0 (ESM-only, breaking) deferred per SPEC §16 #6.
- 2026-07-10 · ESLint 9.39.5 stack from the CV7 template + `eslint-config-prettier` · SPEC §12 "ESLint + Prettier defaults"; ESLint 10 is post-AI-knowledge.
- 2026-07-10 · `@types/node` pinned 24.13.3 · npm's `latest` tag tracks Node 26, not our pinned 24.
- 2026-07-10 · PocketBase v0.39.6 pinned in `scripts/dev.sh`; binary + `pb_data/` gitignored, downloaded on first `npm run dev`.
- 2026-07-10 · DESIGN §3 chip-contrast prose corrected with measured ratios (soft/deep 5.07–9.22:1, espresso-on-core 5.23–7.63:1) · the draft's "≈9:1" claim only holds for lilac; all pairs pass AA.
- 2026-07-11 · Login wordmark is lowercase `stitches` (Baloo 2 700) · resolves the DESIGN §9 ("Stitches") vs §14 (lowercase wordmark everywhere) conflict in favor of §14's more specific typographic rule.
- 2026-07-11 · `lucide-react` pinned 1.24.0 (post-AI-knowledge 1.x major); icon names verified against the installed package — the `Home` icon is now `House` · never trust remembered 0.x lucide names; grep the package's `.d.ts` first.
- 2026-07-11 · Session 0.2 deps pinned: `pocketbase` 0.27.0, `react-hook-form` 7.81.0, `zod` 4.4.3, `@hookform/resolvers` 5.4.0 (zodResolver accepts zod 4 via standard-schema) · SPEC §5 stack, exact pins per `.npmrc`.
- 2026-07-11 · Tailscale admin DNS: **"Override DNS servers" enabled** (global nameserver 1.1.1.1) · iOS let a non-Tailscale resolver win despite "Use Tailscale DNS" (tailscale#12563), so the iPhone got NXDOMAIN for `*.ts.net` and 📱 HTTPS tests were blocked; lives in the admin console, not the repo — don't disable without re-testing MagicDNS on iOS.
- 2026-07-11 · **API rules hardened beyond SPEC §7's literal text** (auth-gate prefix on visibility-aware reads; `:changed` locks on attachment→pattern / entry→project / counter→project; `project.owner` guard on counters create; pattern-link visibility guard on projects create/update) · the literal rules leaked shared records to anonymous requests and let a user re-point owned records into a friend's shared parents.
- 2026-07-11 · patterns.deleteRule gains a `projects_via_pattern` back-relation guard · PB v0.39.6 blocks deletes only for *required* relation references (verified against the pinned binary) and silently auto-unlinks optional ones — SPEC §7's "deleting a pattern with linked projects is blocked" needed a rule to be true.
- 2026-07-11 · `owner` relations `cascadeDelete: true` on all six library collections · account deletion (superuser-only) wipes that person's content; SPEC was silent.
- 2026-07-11 · PB v0.39.6 has **no schema-level field defaults** · SPEC §7's "default …" selects ship optional; empty visibility never matches `"friends"` so records are private-by-default, and the Session 1.2 forms supply display defaults (crochet/saved/planned/private).
- 2026-07-11 · `thumbs ["100x100","400x0","800x0"]` provisioned on every image file field (incl. `projects.cover`, `journal_entries.photos`) · PB serves `?thumb=` variants only for pre-declared sizes; SPEC §8's "lists never load originals" silently breaks otherwise.
- 2026-07-11 · **Rich text via TipTap 2.27.2** (`@tiptap/react`+`starter-kit`+`pm`) + `dompurify` 3.4.12 for PB `editor` fields · Zara chose a real editor over a textarea for 1.2; 2.x is the AI-known major (3.x is mid-2025+; npm keeps a `v2-latest` tag), dompurify sanitizes stored HTML on render.
- 2026-07-11 · `cyc_8` UI label is **"8 · Jumbo+"** (Zara's pick) · the schema value sits past the official CYC 0–7 standard; label lives in `web/src/lib/cyc.ts`, schema untouched.
- 2026-07-11 · Dock ➕ navigates to `/patterns/new` (interim) · the real multi-door quick-add sheet waits for a second door (file 1.3, link 3.2).
- 2026-07-11 · `browser-image-compression` runs with `useWebWorker: false` · its worker mode `importScripts()`s the library from the jsDelivr CDN (options.libURL default) — a hidden network dependency a self-hosted app must not have (same reasoning as self-hosted fonts); its output is also a Blob with `.name` patched on, so the pipeline rewraps into a real `File` or FormData uploads it named "blob".
- 2026-07-11 · Empty-search microcopy: "Nothing matches, yet — try loosening a filter." · DESIGN §11 gives only the empty-library line verbatim; approved with the p04 plan.
- 2026-07-12 · Session 1.3 scope adds `pattern_text` (typed pattern) editing to the vault card (Zara's pick with the p05 plan) · the brief listed files only, but the copyright vault (SPEC §1) explicitly covers typed instructions and TipTap was already integrated; one text-only `pattern_attachments` record per pattern, one record per uploaded file otherwise.
- 2026-07-12 · `pdfjs-dist` pinned 5.7.284 (registry latest is 6.1.200, a post-AI-knowledge major) · same hold policy as Vite 7/TS 5.9/ESLint 9; API verified against the installed `.d.ts` — 5.x `render()` wants `canvas` (not `canvasContext`, which requires `canvas: null`); worker self-hosted via Vite `?url` import (no CDN, same reasoning as `browser-image-compression`'s `useWebWorker: false`).
- 2026-07-12 · npm-script env loading fixed to `--env-file-if-exists=.env` · `--env-file-if-missing` never existed in Node (the v22.9+ flag is `-if-exists`; confirmed against node 24.18.0 `--help`); the 1.1-era `seed`/`verify:rules` scripts had never run via npm directly — every green run went through `verify-fresh.sh`, which sources `.env` itself.
- 2026-07-13 · `motion` pinned 12.42.2 (motion.dev; only `motion/react` imported) for the finish confetti · Zara's pick with the p06 plan (SPEC §5 lists motion; 12.x is the AI-known major); confetti must ALSO be JS-gated via `useReducedMotion()` — base.css's reduced-motion kill only reaches CSS animations, never JS-driven motion.
- 2026-07-13 · `/projects` reconciles PLAN 2.1 "grouped by status" with DESIGN §9 "status chips filter": grouped sections (in_progress → planned → hibernating → finished → frogged) plus a single-select chip filter in `?status=` · Zara's pick with the p06 plan.
- 2026-07-13 · Journal entries get full CRUD in 2.1 (edit-in-place + plain-confirm delete), not compose-only · Zara's pick with the p06 plan; reuses the composer, so the cost was one prop.
- 2026-07-13 · Status sheet applies instantly on tap — no FilterSheet-style draft/Apply · a status pick is one complete intent (single-select radio), and frogged is a reversible select, not a destructive action; approved with the p06 plan.
- 2026-07-13 · Date defaults are minimal-assumption: flip→finished sets `finished_on = today` only-if-empty, un-finishing leaves it untouched, and status flips never touch `started_on` (the create form pre-fills today, visible and clearable) · approved with the p06 plan.
- 2026-07-13 · Finished prompt is a dialog ("Finished! ☆ · Add a finished photo to the journal?") that opens the journal composer — never a programmatic file-input `.click()` · iOS Safari drops async clicks after state-driven mounts; one honest extra tap is reliable; approved with the p06 plan.
- 2026-07-13 · New microcopy: journal-feed empty "No entries yet — this project's story starts here." and project visibility helper "Friends can see this project and its journal — counters stay yours." · DESIGN §11/§9 give only the projects-list and pattern-toggle lines verbatim; approved with the p06 plan.
- 2026-07-13 · `projectKeys` + project read hooks moved `features/patterns/queries.ts` → `features/projects/queries.ts`, import sites fixed directly (no re-export shims) · projects became a real feature in 2.1.
- 2026-07-13 · Entry composer uses plain component state, not RHF+zod · both fields (a date and TipTap html) live outside RHF even in PatternForm; the only rule is "a body or a photo before Save".
- 2026-07-13 · Project cards ship without DESIGN §9's slim progress bar · meaningless before counters exist; lands with Session 2.2's counter work.
- 2026-07-13 · SPEC §11's "ops apply to the TanStack cache instantly" implemented as a pending-ops overlay folded over a server-truth cache (`foldValue` in lib/outbox.ts) · identical instant UX, but literal cache mutation would visibly revert pending taps on any refetch/invalidation and race realtime echoes; approved with the p07 plan.
- 2026-07-13 · Coalescing upgraded to per-counter compaction (an inc merges into its counter's last queued op; a set deletes that counter's prior queued ops; the in-flight op is never touched) · linked resets interleave `set(B,0)` between incs, so SPEC's literal "consecutive incs" would never coalesce; queue stays ≤2 ops/counter offline and ten airplane taps flush as one `{"value+":10}`.
- 2026-07-13 · At-least-once flush accepted (an inc can double only if the response is lost mid-flight; sets are idempotent) · PB has no idempotency keys, and a sent-flag forces a resend-or-drop dilemma where drop loses taps — worse than a visible, manually fixable double.
- 2026-07-13 · `visibilitychange` added to SPEC §11's flush triggers; flusher PATCHes pass `requestKey: null` · iOS fires visibilitychange far more reliably than focus on PWA return; the SDK's default auto-cancel key (METHOD+path) would let a counter meta-edit PATCH silently cancel an in-flight flusher PATCH on the same record.
- 2026-07-13 · Flusher runs under a Web Lock (`ifAvailable`, module-boolean fallback pre-iOS-15.4) with re-read-from-storage before every send, plus a `storage` listener · two tabs share the persisted queue; double-flushing `value+` ops would double-count.
- 2026-07-13 · Reset gets a one-tap confirm sheet ("Reset {label} to 0?"); card ± shares the surface's tap semantics (+1 fires linked resets, the mint star stays surface-only); the /projects progress bar reads the project's *primary* counter = oldest with a target; the numeral zone is the swipe pager (strip is tap-to-switch) · Zara's picks with the p07 plan.
- 2026-07-13 · The surface strip shows all counters with the active one highlighted, not only "the other counters" (DESIGN §10 literal) · a steady chip row beats one that reshuffles on every switch; live values still satisfy the intent.
- 2026-07-13 · New microcopy: counters-card empty "Nothing counted yet — rows, repeats, anything worth keeping track of."; surface empty "Nothing to count yet — add one and keep your place."; captions "{A} +1 resets {B}" / "Resets with {A}"; sync stitch "Syncing — your taps are saved"; delete confirm "Delete this counter? Its count goes with it." · DESIGN §10/§11 give only the caption shape verbatim; approved with the p07 plan.
- 2026-07-13 · Haptic tick = a hidden native `<input type="checkbox" switch>` clicked synchronously inside the tap gesture (`features/counters/haptics.tsx`), pref-gated, default off · SPEC §1 is right that iOS has no Vibration API — the iOS 17.4+ switch-toggle haptic side effect is the only thing that works on an iPhone, which is exactly why DESIGN §9 calls the toggle "experimental"; Zara's pick with the p08 plan; on-device proof waits for the phase walk.
- 2026-07-13 · Hero Journal button opens EntryComposer in a bottom sheet on Home (`JournalQuickSheet`), not a navigation · DESIGN §9 says "quick entry" — the composer was already reusable and a page load isn't quick; Zara's pick with the p08 plan.
- 2026-07-13 · Home's doors row ships two doors (Add a file · Type it in) until 3.2 · DESIGN §9 lists three, but a dead Paste-a-link door can't ship before the importer exists; pick logic shared with the dock ➕ sheet via `features/patterns/useAddFileDoor.ts`; Zara's pick with the p08 plan.
- 2026-07-13 · Home hero counter = the project's primary (oldest with a target, the p07 rule) falling back to the oldest counter at all; Count buttons link `?c=<heroId>` · a WIP without targets still deserves a live value; the surface validates the param anyway.
- 2026-07-13 · `applyServerCounter`/`removeCounterFromCache` mirror into `counterKeys.mine` (delete's owner falls back to the authStore id) · the queryClient's 30 s staleTime let Home serve pre-tap values after pending ops drained; funneling both lists keeps every counter view live with zero screen-specific plumbing.
- 2026-07-13 · `features/home/` and `features/settings/` folders added · SPEC §6's feature list predates these screens becoming real features.
- 2026-07-13 · App version surfaced via Vite `define` from `web/package.json`, bumped to 0.2.3 (phase.session numbering); `resolveJsonModule` added to tsconfig.node.json · the settings footer needs one source of truth and no plumbing existed.
- 2026-07-13 · New microcopy (p08): home partial-empty "Nothing on the hook right now." + "Start a project"; new-user "Start your library" + "Save a pattern you love — the hook comes later."; hero hint "No counters yet — the Count button will set one up."; settings toasts "Saved ♡" / "Password changed ♡" / "Password changed — log in with your new one." / inline "That doesn't match your current password."; prefs captions incl. "A tiny tick with every tap. Experimental — newer iPhones only."; footer "stitches v0.2.3 · made with ♡" · DESIGN §9/§11 give only "start your library" verbatim; approved with the p08 plan.
- 2026-07-13 · Importer fetches via explicit `undici` 7.28.0 — fetch + a per-hop Agent whose `connect.lookup` returns only the pre-validated addresses (checked IP ≡ dialed IP) · closes SPEC §10's resolve-then-fetch TOCTOU/DNS-rebinding window; Node's global fetch accepts no dispatcher; Zara's pick with the p09 plan.
- 2026-07-13 · Importer token validation is **POST** `/api/collections/users/auth-refresh` · SPEC §10 says GET; PB v0.39's API and the 0.27.0 SDK both use POST.
- 2026-07-13 · Importer held majors: @fastify/rate-limit 10.3.0 (11.x is 2026-06, post-knowledge), esbuild 0.25.12 (0.26+ post-knowledge; esbuild minors are breaking), undici 7.28.0 (8.x post-knowledge; matches OGS's ^7.28.0 so the tree dedupes); fastify 5.10.0 · OGS 6.12.0 · ipaddr.js 2.4.0 · tsx 4.23.1 as the dev-only runner (Zara's pick with the p09 plan; prod always runs the esbuild bundle).
- 2026-07-13 · SSRF guard allows only ipaddr.js's `unicast` range, unwrapping IPv4-mapped IPv6 before the check · stricter than SPEC §10's enumerated deny-list; `[::ffff:127.0.0.1]` blocks as loopback (the right reason) while a mapped public IP still passes.
- 2026-07-13 · Importer rate limiter runs at `onRequest` after the auth hook, keyed by user id · the plugin README's `preHandler` example would let malformed-body 400s bypass the 20/min budget (validation runs before preHandler).
- 2026-07-13 · Importer maps PB-unreachable to 401 with a distinct `pb_unreachable` log event · SPEC §10's error contract has no 5xx; "cannot confirm your token" is the honest nearest code, and the log event keeps a PB outage from reading as a bad-token storm.
- 2026-07-13 · Extract contract split: `source_url` = the requested URL (credential-stripped, pre-redirect); `canonical_url` = `og:url` resolved against — else equal to — the final post-redirect URL · SPEC §10's normalize step doesn't define the distinction.
- 2026-07-13 · `scripts/verify/importer-check.mjs` + `npm run verify:importer` added as the permanent Session 3.1 regression (rules-check conventions; internet-dependent cases labeled `[net]`) · Zara's pick with the p09 plan.
