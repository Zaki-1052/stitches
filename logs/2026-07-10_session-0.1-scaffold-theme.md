# 2026-07-10 — Session 0.1: Scaffold & theme

## What was done

- Pre-session housekeeping: docs renamed to `docs/SPEC.md` / `DESIGN.md` / `PLAN.md` /
  `handoff.md`, `git init`, Node pinned 24.18.0 via nvm (Zara).
- npm-workspaces repo: root `package.json` (workspaces `web`, `importer`), `.nvmrc`,
  `.npmrc` (`save-exact=true`), `.gitignore`, `.prettierrc`, root `CLAUDE.md`, `PLAN.md`.
- `web/` scaffolded with pinned `create-vite@7.1.3` (react-ts template, Zara ran it), then:
  exact-pinned all deps, added Tailwind 4 + daisyUI 5 + react-router + @fontsource fonts,
  `theme.css` verbatim from DESIGN §2, vite proxies (`/api` `/_` → 8090, `/import` → 8095,
  `allowedHosts: ['.ts.net']`), viewport meta with `viewport-fit=cover`, router with a
  throwaway `/tokens` demo page (swatches, chip states, buttons, `stitchesdim` panel),
  eslint-config-prettier appended to the template flat config, boilerplate deleted.
- `importer/` stub workspace (zero deps, Phase 3). `pb/pb_migrations/` created with README.
- `scripts/dev.sh`: platform-detect, downloads PocketBase v0.39.6 on first run, starts
  PB (127.0.0.1:8090, `--dir pb/pb_data --migrationsDir pb/pb_migrations`) + Vite together.
- `scripts/verify/contrast.mjs`: zero-dep WCAG gate over every DESIGN §3 token pair.
- `docs/DECISIONS.md` created and seeded with nine dated entries.

## Verification results

- Contrast: **32/32 pairs pass AA** (tightest: ink-muted/base-300 4.81:1, warning pair 4.94:1).
- `eslint` exit 0 · `tsc -b` exit 0 · hex-grep over `web/src` zero hits.
- `npm ls`: vite 7.3.6, plugin-react 5.2.0, eslint 9.39.5, TS 5.9.3, react 19.2.7,
  react-router 7.18.1, tailwindcss 4.3.2, daisyui 5.6.16 — exact, no conflicts.
- `npm run dev` boots PB + Vite; `/tokens` renders both themes (Zara, in browser).

## Decisions made

- **Pin to AI-known majors** (the session's governing rule): Vite 7 not 8, ESLint 9 not 10,
  TS 5.9.3 not 6/7, React Router 7.18.1 not 8 — AI can't assist with post-knowledge breaking
  majors. Deviates from SPEC §5 "Vite: current major"; logged in DECISIONS.md.
- `@vitejs/plugin-react` held at 5.2.0 — 6.x peer-requires Vite ^8 only (the era-pin trap).
- `@types/node` 24.13.3 (npm `latest` tracks Node 26, not our pinned 24).
- Nested `web/node_modules` + lockfile (from a pre-workspaces install) removed; the root
  lockfile is the only one.
- tsconfigs already ship explicit `"strict": true` — no edit needed.

## Open items

- First commit (Zara; paste the 32/32 contrast output into the commit body — acceptance item).
- Session 0.2 next: users migration, PB SDK singleton + `authRefresh`, login screen, app
  shell/dock, seed script, superuser setup — first 📱 tailscale device check happens there.
- daisyUI theme prose claims "≈9:1" for soft/deep chip pairs; real ratios run 5.1–9.2:1.
  All pass AA — noted only in case DESIGN's prose ever gets treated as a spec value.

## Key file paths

`web/src/styles/theme.css` · `web/vite.config.ts` · `web/src/main.tsx` ·
`web/src/routes/TokensPage.tsx` · `scripts/dev.sh` · `scripts/verify/contrast.mjs` ·
`CLAUDE.md` · `docs/DECISIONS.md` · `PLAN.md`
