# 2026-07-11 · Session 0.2 — Auth & App Shell

Built login → themed shell on top of the 0.1 scaffold. Auth is gated by PocketBase rules; the
client wrapper is UX only.

## What was done

- **Migration** `pb/pb_migrations/1783806920_configure_users.js` — configures the built-in `users`
  auth collection (SPEC §7/§9): list/view `@request.auth.id != ""`, **create & delete locked**
  (invite gate), update self-only; password-only (oauth2/otp/mfa off); **90-day token**
  (7,776,000 s); `name` required; `avatar` ≤5 MB. Idempotent (edits in place); has a `down` that
  restores v0.39.6 fresh-install defaults.
- **PB client + auth** — `web/src/lib/pb.ts` (same-origin singleton) and `web/src/lib/auth.tsx`
  (`AuthProvider`/`useAuth` via `useSyncExternalStore` over `pb.authStore`; boot-time
  `authRefresh()` → clear-on-failure per SPEC §9).
- **Login** — `web/src/routes/LoginPage.tsx` (react-hook-form + zod, gentle inline error, iOS
  input rules) + `web/src/components/YarnBall.tsx` (hand-drawn mark, DESIGN §6; token fills only).
- **Shell** — `AppShell` / `AppHeader` (greeting + avatar → settings) / `Dock` (daisyUI `dock`,
  Home·Library·➕·Projects, raised 56px blue ➕) / `BackBar` (in-app back, cold-start fallback) /
  `ProtectedRoute` (splash while booting, redirect when logged out). Stubs: Home, Library,
  Projects, Settings (subpage w/ back + logout). `styles/base.css` for global iOS hygiene.
- **Routing** — `main.tsx` rewired: public `/login`; everything else under `ProtectedRoute`
  (`/` → AppShell with Home/Library/Projects/tokens children; `/settings` as a dockless subpage).
  Deleted `web/src/App.tsx` (superseded by `AppShell`).
- **Seed + docs** — `scripts/seed.mjs` (env-only creds, superuser-authed, idempotent), root
  `package.json` `seed` script, fresh root `README.md` (superuser + seed + tailscale), two
  `DECISIONS.md` lines.

## Decisions made

- Wordmark **lowercase `stitches`** (DESIGN §14 over §9). Logged.
- **rhf + zod** adopted now for the login form (SPEC §5 stack).
- Seed credentials **entirely via env vars** — nothing hardcoded.
- **Settings is a dockless pushed subpage** (BackBar), distinct from dock destinations — this is
  the canonical subpage/back-affordance pattern. TanStack Query deferred to 1.1.
- Verified the PB v0.39.6 JSVM migration API against `pb/pb_data/types.d.ts` + Context7 JSVM docs
  before writing (create-vs-edit, nested option paths, `createRule = null` = locked).

## Verification

- `npm run lint` clean · `npx tsc -b web` exit 0 · `npm run verify:contrast` 32/32 AA.
- On-device run (Zara): migration confirmed applied against the live DB (createRule/deleteRule
  locked, token 7776000, name required, avatar 5 MB); seed created cece + friend; invite gate
  returns 403 to public create.
- **Bug found & fixed during login testing:** `auth.tsx` fed `pb.authStore.record` into
  `useSyncExternalStore`, but the SDK getter returns a fresh object each call → React
  "getSnapshot should be cached" infinite loop → white screen on *successful* login. Replaced with
  a `useState` + `pb.authStore.onChange` subscription (state updates only on real store mutations).
- Session 0.2's two acceptance boxes in `docs/PLAN.md` stay unchecked pending the final 📱 pass.

## Open items

- Migration applies on `serve`; if any nested-option write is off, PB logs loudly on boot (the one
  residual risk — direct-mutation idiom is standard PB v0.23+, verified against types).
- ➕ dock button is a styled placeholder (quick-add sheet = Phase 1).
- App icon / manifest / full 13-item hygiene audit remain Phase 4.

## Key file paths

`pb/pb_migrations/1783806920_configure_users.js` · `web/src/lib/{pb.ts,auth.tsx}` ·
`web/src/components/{YarnBall,ProtectedRoute,AppShell,AppHeader,Dock,BackBar}.tsx` ·
`web/src/routes/{LoginPage,HomePage,LibraryStub,ProjectsStub,SettingsStub}.tsx` ·
`web/src/styles/base.css` · `web/src/main.tsx` · `scripts/seed.mjs` · `README.md`
