# 2026-07-11 · Session 1.2 — Pattern CRUD

Stitches now works as a manual-entry library: library list with URL-param filters, create/edit
form, §8 image pipeline, pattern detail, delete. Plan: `logs/p04-session-1.2-pattern-crud.md`
(approved; three scope questions answered by Zara up front).

## What was done

- **Deps (Zara installed, exact pins):** `@tanstack/react-query` 5.101.2,
  `browser-image-compression` 2.0.2, TipTap 2.27.2 (`@tiptap/react`/`starter-kit`/`pm`),
  `dompurify` 3.4.12.
- **lib/** — `schema.ts` (zod form schemas as the PB mirror, record types, select consts +
  labels, `patternToFormValues`), `hooks.ts` (13-row mm↔US table, `formatHook` → "5.0 mm · H-8"),
  `cyc.ts` (CYC labels incl. "8 · Jumbo+").
- **features/shared/** — `queryClient`, `errors.ts` (PB error normalizer + RHF bridge),
  `toast.tsx` (top, 2.5 s, one at a time), `imagePipeline.ts` (≤2000 px WebP ~0.85, JPEG fallback
  by checking the *output MIME*, canvas fallback, per-file failures), `patchColors.ts`.
- **features/patterns/** — `queries.ts` (list hard-scoped `owner = viewer`; detail expands tags;
  own tags; §7.9 finished-pattern ids; delete pre-check), `mutations.ts` (full saves invalidate;
  shelf/visibility quick-taps optimistic), `formData.ts` (send-every-field builder: `''`/`'0'`
  clears, `owner` only on create, `photos+`/`photos-` on edit), `urlParams.ts` (defensive
  parse/serialize), and 16 components (form leaves incl. TipTap `NotesEditor` + inline-create
  `TagPicker`; library leaves incl. draft-state `FilterSheet`; detail leaves incl. blocked-path
  `DeleteConfirmDialog`).
- **routes/** — `LibraryPage` (replaces deleted `LibraryStub`), `PatternFormPage`
  (new + edit, owner-guard redirect), `PatternDetailPage` (read-only for friends' shared).
- **Shell** — `main.tsx` (QueryClientProvider, ToastProvider, richtext.css, 3 dockless
  `/patterns/*` subroutes), `BackBar` optional `right` slot, Dock ➕ → `/patterns/new`.
- **Docs** — 6 lines appended to `docs/DECISIONS.md`.

## Decisions made

- Zara: rich-text notes (TipTap 2.x, not a textarea) · cyc_8 = "8 · Jumbo+" · ➕ interim wiring.
- Library URL state always `replace`s history — one Library entry; back restores filters, exits in
  one press.
- Image fields live outside zod/RHF (zodResolver adopts parsed output; `z.object` strips unknown
  keys → would drop File state); read at submit.
- `browser-image-compression`: worker mode disabled (CDN importScripts) and output rewrapped into
  a real `File` (library returns a Blob with `.name` patched on) — verified in its source.

## Verified

- `npx tsc -b web --force` exit 0 · `npm run lint` clean · `vite build` succeeds.
- No migrations/rules/seed touched → no `verify:rules` gate required.
- PLAN.md 1.2 boxes NOT ticked yet — awaiting Zara's 📱 pass (checklist in p04 §Verification);
  that pass also covers Session 0.2's still-unchecked 📱 box.

## Open items

- Bundle is ~906 kB minified (TipTap-heavy) — route-level splitting is Session 4.2's explicit
  "bundle sanity" item.
- Sticky search bar sits at viewport top-0; fine in Safari-tab dev, re-check under standalone PWA
  safe-area in Phase 4.
- iOS native search-clear ⓧ may double with our custom clear button — cosmetic, check on device.

## Key file paths

`web/src/lib/{schema,hooks,cyc}.ts` · `web/src/features/shared/{queryClient,errors,toast,imagePipeline,patchColors}` ·
`web/src/features/patterns/{queries,mutations,formData,urlParams}.ts` ·
`web/src/features/patterns/components/*` (16 files) ·
`web/src/routes/{LibraryPage,PatternFormPage,PatternDetailPage}.tsx` · `web/src/main.tsx` ·
`web/src/styles/richtext.css` · `docs/DECISIONS.md` · `logs/p04-session-1.2-pattern-crud.md`
