# 2026-07-13 — Session 2.1: Projects & journal

## What was done

- **Data layer** (`web/src/features/projects/`): `queries.ts` (projectKeys/entryKeys +
  `useProjects`/`useProject`/`useJournalEntries` sorted `-entry_date,-created`; absorbed
  `useFinishedPatternIds`/`useLinkedProjects` from patterns), `mutations.ts` (FormData
  create/update, optimistic `useQuickUpdateProject`, deletes, entry CRUD), `formData.ts`
  (`summary` never sent by forms; entry edit omits `owner`/`project` per rule locks),
  `urlParams.ts` (`?status=` whitelist), `status.ts` (STATUS_PATCH, normalizeStatus).
- **lib**: `dates.ts` (slice-not-parse rule for PB dates — tz-safe backdating; `todayLocalISO`,
  `toPbDate`, `formatShortDate`); `schema.ts` gained PROJECT_STATUSES/labels, ProjectRecord,
  JournalEntryRecord, projectFormSchema/defaults/toFormValues, `ProjectLinkRecord.name`.
- **Components** (`features/projects/components/`): StatusChip/StatusSheet (instant-apply
  bottom sheet, frogged caption)/StatusPill; ProjectCard; ProjectsEmptyState ("Nothing on the
  hook. Chain on!"); ProjectForm (pattern select w/ whitelisted `?pattern=` prefill, date fields
  with clear buttons, cover via ThumbnailField); SummaryCard (edit-in-place); EntryComposer
  (create+edit, needs body or photo); EntryCard; JournalFeed (date-grouped, composer on top);
  StarConfetti (24 patch-color SVG stars via motion/react, ≤1.5 s, transform/opacity only);
  FinishedPromptDialog; ConfirmDeleteDialog (generic).
- **Routes**: ProjectsPage (grouped sections + chip filter; stub deleted), ProjectFormPage,
  ProjectDetailPage (finished-flip state machine: mutation `onSuccess` → confetti once →
  prompt → composer; `useReducedMotion` skips confetti, keeps everything instant); main.tsx
  wired `/projects/new` before `/projects/:id`, `/projects/:id/edit`.
- **Patterns side**: PatternDetailPage projects section + owner-only "Start a project"
  (`/projects/new?pattern=`); PhotosField gained `max`/`label` props, SaveBar `label`,
  VisibilityToggle `helperText` (patterns call sites updated, zero behavior change);
  `usePatternOptions()` feeds the project form.

## Decisions made

Ten lines appended to `docs/DECISIONS.md` (2026-07-13): motion 12.42.2 pin + JS reduced-motion
gate; grouped-sections+chips reconciliation; full entry CRUD; instant-apply status sheet;
minimal-assumption date defaults; finished-prompt dialog (no programmatic file-input click);
two new microcopy lines; projectKeys move; composer without RHF; no progress bar until 2.2.

## Verification

- Claude: `npx tsc -b` (web) exit 0 · `npm run lint` exit 0, no warnings · motion/react API
  (`motion` proxy, `useReducedMotion`) verified against installed `.d.ts`.
- Zara ran `npm install motion@12 --workspace=web` (saved exact 12.42.2).
- 📱 Zara's on-device walk green (2026-07-13): full loop w/ confetti once → prompt · Reduce
  Motion ON → no confetti, instant updates · backdated sorting · chips/URL back-button ·
  frogged microcopy · date inputs. All three docs/PLAN.md §2.1 acceptance boxes ticked.
- No `pb/`, rules, or seed files touched — `verify:rules` not required by the regression gate.

## Open items

- Counters card + `/projects/:id/count` (Session 2.2); project-card progress bar rides along.
- Friends starting projects off shared patterns + shared-feed rendering (Phase 4).
- Root `PLAN.md` → archive to `logs/p06-session-2.1-projects-journal-plan.md` once 📱 green.

## Key file paths

`web/src/features/projects/{queries,mutations,formData,urlParams,status}.ts` ·
`web/src/features/projects/components/*` (13 files) · `web/src/lib/{dates,schema}.ts` ·
`web/src/routes/{ProjectsPage,ProjectFormPage,ProjectDetailPage}.tsx` · `web/src/main.tsx` ·
`web/src/routes/{LibraryPage,PatternDetailPage}.tsx` · `web/src/features/patterns/queries.ts` ·
`web/src/features/patterns/components/{PhotosField,SaveBar,VisibilityToggle,PatternForm}.tsx`
