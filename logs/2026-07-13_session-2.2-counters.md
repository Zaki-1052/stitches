# 2026-07-13 — Session 2.2: Counters

## What was done

- **Outbox** (`web/src/lib/outbox.ts`, SPEC §11): localStorage-persisted FIFO
  (`stitches:outbox:v1`, ownerId-tagged) of `inc`/`set` ops; per-counter compaction;
  display = `foldValue(pending, serverTruth)` overlay (not cache mutation — see DECISIONS);
  single-flight flusher under a Web Lock (`requestKey: null`, `value+` atomic incs,
  write-response-then-remove-op so the numeral never dips; retry on network/401, drop+toast on
  4xx); triggers: append/online/focus/visibilitychange/10 s tick/boot via `initOutbox()` in
  main.tsx (module scope — StrictMode-proof, flushes on reopen without visiting a counter).
- **Data layer**: `CounterRecord` (schema.ts); `features/counters/queries.ts` (counterKeys,
  `useCounters` created-asc, `useMyCounters`, `applyServerCounter` with monotonic `updated`
  guard, `removeCounterFromCache`); `mutations.ts` (create sends explicit `value: 0`;
  meta-update never sends value/project/owner; delete drops queued ops); `actions.ts`
  (`buildTapOps`: inc + `set(B,0)` per `resets_with` dependent, increments only;
  `crossesTarget`); `realtime.ts` (`useCountersRealtime`: filtered subscribe — the app's
  first — cancelled-flag cleanup; verified vs pocketbase 0.27.0 .d.ts).
- **Project detail**: CountersCard at the 2.1 placeholder (owner-only rows: label, folded
  value, ± through the outbox, link glyph; ghost Add row; big Open counter) + CounterSheet
  (create/edit label/target/resets_with same-project select, delete confirm) + realtime.
- **Surface** (`web/src/routes/CounterPage.tsx`, `/projects/:id/count`, DESIGN §10): 56 px bar
  (back · name · moon · Coffee wake toggle · edit); scroll-snap numeral pager (validated `?c=`
  is truth, settled-swipe writes it back `replace`), dots, tap-to-switch strip; progress bar
  "12 of 48 rows"; linked captions both directions; −1/edit/reset row (EditValueSheet,
  ResetConfirmSheet); +1 tap zone `clamp(120px,22dvh,180px)` + safe-area; numeral roll-pop,
  8 % primary pulse, TargetStar (one mint star, local crossings only), SyncStitch while queue
  non-empty; sr-only `aria-live` region; offline-reopen error state; all JS motion
  `useReducedMotion`-gated.
- **Prefs groundwork** (`web/src/lib/prefs.ts`): `stitches:prefs:v1`
  {counterDim, counterWakeLock, hapticTick} + `usePref`; dim = `data-theme="stitchesdim"`
  subtree swap with 250 ms color cross-fade; `useWakeLock` re-acquires on visibilitychange.
- **/projects**: ProjectCard slim progress bar (deferred from 2.1) via one `useMyCounters`
  query; primary counter = oldest with a target.

## Decisions made

Nine lines appended to `docs/DECISIONS.md` (2026-07-13): overlay model; per-counter
compaction; at-least-once flush; visibilitychange trigger + `requestKey: null`; Web-Locks
multi-tab guard; Zara's p07 picks (reset confirm sheet, card-± semantics, primary-counter
progress bar, numeral-zone pager); strip shows all counters; new microcopy.

## Verification

- Claude: `npx tsc -b` exit 0 · `npm run lint` clean · `npm run build` green (chunk-size
  warning pre-dates this session; splitting is 4.2's brief) · SDK subscribe-options/
  requestKey/lucide names verified against installed .d.ts.
- No `pb/`, rules, or seed changes — `verify:rules` not required by the regression gate.
- Zara is holding off on-device/two-browser testing until Phase 2/3 is complete; the four
  PLAN §2.2 acceptance boxes stay unticked. Plan archived to
  `logs/p07-session-2.2-counters-plan.md` at her request (2026-07-13).

## Open items

- Deferred to the end-of-Phase-2/3 walk: two-browser live sync · offline burst → reload →
  reconnect exact value · 📱 the four PLAN §2.2 acceptance boxes.
- tabular-nums jitter check on device (DESIGN §4's fixed-width fallback if Baloo misbehaves).
- 2.3 consumes prefs (wake-lock default, dim memory, hapticTick toggle) + home hero's
  "live primary counter value" (notion established here).

## Key file paths

`web/src/lib/{outbox,prefs,schema}.ts` · `web/src/features/counters/{queries,mutations,
actions,realtime,useWakeLock,useOutboxErrorToasts}.ts` ·
`web/src/features/counters/components/{CountersCard,CounterSheet,EditValueSheet,
ResetConfirmSheet,TargetStar,SyncStitch}.tsx` · `web/src/routes/{CounterPage,ProjectsPage,
ProjectDetailPage}.tsx` · `web/src/features/projects/components/{ProjectCard,StarConfetti}.tsx`
· `web/src/main.tsx`
