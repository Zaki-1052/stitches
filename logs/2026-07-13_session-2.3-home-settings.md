# 2026-07-13 — Session 2.3: Home & settings

## What was done

- **Settings** (`routes/SettingsPage.tsx` replaces the stub; `features/settings/components/`):
  ProfileCard (name RHF+zod; avatar through the §8 pipeline with ThumbnailField's
  unchanged/new/removed semantics — unchanged never sends the field; the SDK's authStore sync
  updates AppHeader for free) · PasswordCard (old+new+confirm; update → re-auth with the new
  password since PB rotates the token key; wrong-old inline error; re-auth failure clears the
  store honestly) · CounterPrefsCard (wake-lock / dim / hapticTick toggles over lib/prefs,
  instant, no save button) · sign out · version footer.
- **Home** (`routes/HomePage.tsx` rewrite; `features/home/components/`): HeroCard (cover 800x0,
  name, pattern, live folded counter value + progress bar, Count → `/count?c=<heroId>`,
  Journal); snap-scroll row for multiple WIPs (86% cards, peek, no URL sync); DoorsRow (two
  doors sharing the sheet's extracted logic); RecentPatternsStrip (`useRecentPatterns(8)` +
  made-✓); state matrix — WIP → heroes · content-but-no-WIP → stitch-border ghost card ·
  truly-new user → HomeEmptyState with the new YarnBasket illustration (drawn per DESIGN §6);
  JournalQuickSheet hosts EntryComposer in a modal-bottom dialog, remounting per open.
- **Counter-cache staleness fix** (`features/counters/queries.ts`): `applyServerCounter` /
  `removeCounterFromCache` now mirror into `counterKeys.mine` — without it, Home showed
  pre-tap values for up to 30 s (global staleTime) once pending ops drained.
- **Haptic tick** (`features/counters/haptics.tsx` + CounterPage `tap()`): hidden native
  `<input type="checkbox" switch>` clicked inside the tap gesture, `getPref('hapticTick')`
  gated; +1/−1 only, never resets/edits.
- **Extractions**: `features/patterns/useAddFileDoor.ts` (QuickAddSheet refactored onto it,
  behavior-identical); `useProjects` gained an `enabled` opt for Home's any-projects probe.
- **Version plumbing**: web/package.json → 0.2.3; Vite `define` __APP_VERSION__;
  `resolveJsonModule` in tsconfig.node.json; declaration in vite-env.d.ts.

## Decisions made

Eight lines appended to `docs/DECISIONS.md` (2026-07-13): haptic switch-hack; journal sheet;
two doors until 3.2; hero counter selection + `?c=`; mine-cache mirror; home/settings feature
folders; version via define; the p08 microcopy batch. Zara's three picks were made with the
p08 plan (AskUserQuestion during planning).

## Verification

- Claude: `npx tsc -b` exit 0 · `npm run lint` clean · `npm run build` green ("0.2.3" confirmed
  in the emitted bundle; chunk-size warning pre-dates this session — splitting is 4.2's brief).
  Lucide names (Pencil, FileUp, PenLine) and the SDK's authStore-sync-on-update verified
  against installed packages.
- No `pb/`, rules, or seed changes — `verify:rules` not required by the regression gate.
- Zara tests locally (then 📱 at the phase walk): password change end-to-end, profile saves
  reflected in the header, pref toggles driving the surface, home state matrix, count-then-home
  freshness, doors → pre-filled form, journal sheet entry. Both PLAN §2.3 boxes stay unticked
  until her confirmation; haptics need a real iPhone (iOS 17.4+).

## Open items

- 📱 phase-walk batch: 2.3's two acceptance boxes, haptic tick on device, keyboard-vs-sheet
  and keyboard-vs-password-save checks, snap-row feel.
- Journal sheet abandoned-photo previews aren't revoked on backdrop-close (same as leaving a
  form mid-edit elsewhere) — cosmetic memory nit, noted for the 4.2 polish pass.
- Paste-a-link door slot on Home waits for 3.2.

## Key file paths

`web/src/routes/{HomePage,SettingsPage,CounterPage}.tsx` ·
`web/src/features/settings/components/{ProfileCard,PasswordCard,CounterPrefsCard}.tsx` ·
`web/src/features/home/components/{HeroCard,DoorsRow,RecentPatternsStrip,HomeEmptyState,JournalQuickSheet}.tsx` ·
`web/src/components/YarnBasket.tsx` · `web/src/features/counters/{haptics.tsx,queries.ts}` ·
`web/src/features/patterns/{useAddFileDoor.ts,queries.ts,components/QuickAddSheet.tsx}` ·
`web/src/features/projects/queries.ts` · `web/src/main.tsx` · `web/vite.config.ts`
