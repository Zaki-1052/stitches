# 2026-07-14 — Session 4.1: Friends

## What was done

- **Feed queries** (`web/src/features/friends/queries.ts`): `friendsKeys` + `useFriendsFeed` —
  one queryFn `Promise.all`s friends' shared patterns (`visibility="friends" && owner != me`,
  expand owner) and friends' shared **finished** projects (expand owner+pattern), merged newest
  first (patterns by `created`, FOs by `finished_on` falling back to `updated`; lexical compare
  on PB's UTC strings). No invalidation plumbing on purpose — the viewer can't mutate this data.
- **Feed UI** (`features/friends/components/`): person-first `FeedCard` (owner avatar + name,
  80 px `?thumb=400x0` cover or YarnBall, title, context, "added {date}" / mint
  "finished {date} ☆"), `FriendsEmptyState` (YarnBasket, no CTA), `SharedByChip`.
- **`/friends` route** (`routes/FriendsPage.tsx` under AppShell in `main.tsx`) with sibling-style
  skeleton / inline-error+Retry / empty / list states; **5th dock tab** (`UsersRound`, after
  Projects — ➕ stays the center child of five).
- **Shared `<Avatar>`** (`components/Avatar.tsx`) extracted from AppHeader (which now uses it and
  loads `?thumb=100x100` instead of the original); ProfileCard deliberately untouched (its img
  is a three-state upload preview). `UserRecord` added to `schema.ts`; `expand.owner` typed on
  Pattern/ProjectRecord.
- **Detail pages**: `usePattern`/`useProject` expand `owner`; non-owners get the SharedByChip
  (patterns: in the shelf-pill slot; projects: under the status row); `useCountersRealtime`
  owner-gated (was subscribing rule-scoped-to-silence for viewers).
- **Migration** `pb/pb_migrations/1784093202_users_avatar_thumbs.js` — `users.avatar` thumbs
  `['100x100']` (SPEC §8 lists-never-load-originals; PB lazy-generates, no backfill).
- `web/package.json` → 0.4.1 · seven DECISIONS.md lines · plan archived as
  `logs/p11-session-4.1-friends-plan.md`.

## Decisions made

Seven lines appended to `docs/DECISIONS.md` (2026-07-14): feed scope finished-only + merged
order + own-items excluded; new person-first FeedCard (double-appearance intentional);
shared-by chip via expand owner; avatar thumbs migration + Avatar extraction; realtime
owner-gate; `UsersRound` dock icon; the p11 microcopy set (em-dash-free per Zara's note —
also saved to memory for future copy).

## Verification

- Claude: `npm run lint` clean · `tsc -b` in `web/` exit 0 (one fix: Avatar's user prop takes
  optional name/avatar — the SDK's RecordModel declares neither, only an index signature).
- Zara ran `npm run verify:fresh` (migrations changed → escalated gate): **ALL GREEN**,
  rules-check 105/105 ×2, fresh-DB migration apply + seed clean — `logs/verify-fresh-4.1.txt`
  (2026-07-14). The 📱 walk as user B (feed contents, read-only details, zero
  attachment/counter hints, private-vs-garbage deep links identical, initials fallback,
  footer v0.4.1) is batched with the phase-4 walk. PLAN 4.1 boxes stay unticked until her
  confirmation.

## Open items

- Zara's on-device acceptance walk (above), batched with the phase-4 walk.
- Session 4.2 next sitting: PWA, icon decision, polish sweep (incl. the deferred
  JournalQuickSheet `revokePreview` leak from DECISIONS 2026-07-13).

## Key file paths

`web/src/features/friends/{queries.ts,components/{FeedCard,FriendsEmptyState,SharedByChip}.tsx}`
(new) · `web/src/routes/FriendsPage.tsx` + `web/src/components/Avatar.tsx` (new) ·
`pb/pb_migrations/1784093202_users_avatar_thumbs.js` (new) · `web/src/components/{AppHeader,
AppShell,Dock}.tsx` · `web/src/main.tsx` · `web/src/lib/schema.ts` ·
`web/src/features/{patterns,projects}/queries.ts` · `web/src/routes/{PatternDetailPage,
ProjectDetailPage,LibraryPage}.tsx` · `web/package.json` · `docs/DECISIONS.md`
