# 2026-07-15 — Session 4.2: PWA, icon & polish

## What was done

- **PWA**: `vite-plugin-pwa` 1.2.0 (generateSW, autoUpdate, dev-disabled) in `web/vite.config.ts`; manifest (lowercase "stitches", standalone, `#FAF4E9`); precache `{js,css,html,ico,png,svg,webmanifest}` minus the pdfjs chunk; navigateFallback denylist `/api` `/_` `/import`; ordered runtime rules — `?token=` NetworkOnly → `?thumb=` CacheFirst (30 d/300) → woff2 CacheFirst (365 d/20) → `/api/` NetworkOnly. Rules verified in the built `dist/sw.js`.
- **Icon**: concept A refined into `web/public/favicon.svg` (full-square 512, safe-zone composed, token-mirroring hexes); `web/pwa-assets.config.ts` (padding-0 preset); `npm run icons` scripts (web + root); index.html gets icon links, theme-color, apple metas.
- **Bundle**: `LazyNotesEditor` seam (TipTap → its own 308 kB lazy chunk); RR7 route `lazy` on all dockless pages + `/tokens` (CounterPage + dock tabs stay eager); react/vendor `manualChunks`. Main chunk 1,126 kB → 395 kB (130 kB gzip); no size warning; precache 1.2 MB.
- **Error states**: first `RouteErrorBoundary` ×2 (router root + inside AppShell, dock survives tab crashes).
- **Deferred fixes**: `useRevokeOnUnmount` hook releases photo previews in EntryComposer/PatternForm/ProjectForm/ProfileCard (StrictMode-safe deferred revoke); Library/Projects sticky bars pad `safe-area-inset-top`; WebKit search-ⓧ killed in base.css; `PatternFormPage` remounts via `key={location.key}` (same-path quick-add).
- **Polish**: AppHeader route-aware (greeting Home-only); desktop pass (1080 px frame, dock as lg+ top bar, dockless pages center at max-w-3xl); button press `scale(0.97)` + toast pop using the previously-unused motion tokens; toast re-keys per message.
- **Housekeeping**: version 0.4.2; root `build`/`preview`/`icons` scripts.

## Decisions made

Thirteen lines appended to `docs/DECISIONS.md` (2026-07-15) — headline items: query-param-gated SW caching (PB URLs use opaque collectionIds), single safe-zone icon master, component-level TipTap seam, `/projects/:id` lazy, StrictMode-safe preview revocation, route-aware header.

## DESIGN §12 audit

| # | Item | Status |
|---|---|---|
| 1 viewport · 2 dvh-only · 5 input≥16px · 6 touch-action · 7 tap-highlight · 8 overscroll · 9 back affordance | already done (0.1–2.2 eras) | verified in code this session |
| 3 safe-area on sticky bars | **fixed this session** (dock/counter already had it) |
| 11 transform/opacity only | **this session** (micro-animation pass); 60 fps is 📱 |
| 13 icons + manifest colors | **this session** (pending `npm run icons` outputs) |
| 4 tap targets · 10 hover-optional · 12 keyboard-vs-Save | 📱 device walk only |

## Open items

- **Zara**: `npm run icons` (writes the PNG set into `web/public/`, commit them) · `git rm web/public/vite.svg` · move the four board files: `mkdir -p docs/assets && mv stitches_app_icon_concepts.svg stitches_app_icon_concepts-choices.png stitches_token_palette_board.svg stitches_token_palette_board.png docs/assets/`
- **Zara, device walk** (batched phase-4 acceptance): `npm run dev` + (second terminal) `npm run build && npm run preview` + `tailscale serve --bg 4173` → Add to Home Screen (icon), airplane-mode relaunch (shell paints), §12 walk incl. keyboard-vs-Save, sticky-bars-below-notch, single search-ⓧ, ➕ re-pick on `/patterns/new`, desktop top bar ≥1024 px, footer v0.4.2. All PLAN boxes stay unticked until her confirmation.
- Card-link (`<a>`) press animation deliberately skipped pending the iOS `:active`-on-anchor device check.
- After `npm run icons` lands, re-run `npm run build` once so the PNGs enter the precache manifest.

## Key file paths

New: `web/public/favicon.svg` · `web/pwa-assets.config.ts` · `web/src/features/patterns/components/LazyNotesEditor.tsx` · `web/src/components/RouteErrorBoundary.tsx` · `web/src/features/shared/useRevokeOnUnmount.ts`
Modified: `web/vite.config.ts` · `web/index.html` · `web/src/main.tsx` · `AppShell/AppHeader/Dock.tsx` · `base.css` · `toast.tsx` · `LibraryPage/ProjectsPage/PatternFormPage.tsx` · detail/form Frames · `EntryComposer/PatternForm/ProjectForm/ProfileCard.tsx` · both `package.json`s · `docs/DECISIONS.md`

Verified: `npm run lint` clean · `verify:contrast` 32/32 AA · `npm run build` green, sw.js rules confirmed.
