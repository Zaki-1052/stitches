# 2026-07-19 — Ravelry R1 + R2 + R3 (enrichment, search door, import script)

Built all three RAVELRY.md phases in one session per `plans/p13-ravelry-r1-r2-r3-plan.md`.
Lint + importer typecheck + web tsc green. **Zara confirmed R1 + R2 same day** (see addendum);
PLAN.md R1/R2 boxes ticked, R3 boxes wait on the actual run.

## What was done

- **Migration** `pb/pb_migrations/1784446344_patterns_ravelry.js`: optional `ravelry_id`
  (number, onlyInt) + `ravelry_fetched_at` (date) on `patterns`; down via `removeByName`.
- **Importer R1**: new `importer/src/ravelry/` — `types.ts`, `detect.ts` (permalink matcher),
  `client.ts` (Basic auth, UA, ETag LRU cache, `RavelryApiError`), `map.ts` (CYC table,
  difficulty buckets, smallest crochet hook, trim-everything). `routes/extract.ts` gained the
  branch with logged fall-through; `routes/ravelrySearch.ts` + `app.ts` register
  `POST /import/ravelry/search` inside the auth/rate-limited scope. `config.ts` now requires
  `RAVELRY_BASIC_USER`/`RAVELRY_BASIC` at boot; `scripts/dev.sh` sources `.env` in the
  importer subshell (it never loaded it before — dev would have broken otherwise).
- **Web R1**: `importerClient.ts` gained `RavelryBlock` + `searchRavelry()`; new
  `useUrlImport.ts` holds the generic extract→image→stash→navigate pipeline (paste door now
  delegates); `pendingUrlImport` carries `ravelryProvenance`; `formData.ts` appends the two
  fields on create only; `PatternForm` opens Details on any prefilled detail (dead `mode`
  prop removed); `schema.ts` PatternRecord updated.
- **Web R2**: `/patterns/search-ravelry` route + `RavelrySearchPage.tsx` (submit-on-enter,
  append paging, disclosure line, empty state) + `RavelrySearchCard.tsx` (CDN thumbs, mint
  free pill, YarnBall fallback). DoorsRow → 2×2 with coral Search tile; QuickAddSheet → 4th row.
- **R3**: `scripts/ravelry-import.mjs` (`--smoke`/`--run`, shelf precedence, tag round-robin,
  batch details, idempotent on `ravelry_id`, ~1 req/s, 429 Retry-After honored once) + npm
  scripts `ravelry:import[:smoke]` + `docs/ravelry-import-runbook.md` + `.env.example`
  owner-account vars. Written, not runnable until Cece's key arrives.
- **Verify**: `importer-check.mjs` gained a `[net] ravelry` section (happy path with pinned
  fixture values, garbage permalink → 422 fall-through, search shape); budget note added
  (user 1 now at 18/20).
- **Docs**: 7 DECISIONS lines (2026-07-19); DESIGN §9 three→four doors.

## Decisions made

- 4th door everywhere; coral patches; door order per approved mockup (Search bottom-right).
- `site_name: null` on Ravelry extracts + `source_name: "Ravelry"` (chip stays "ravelry.com").
- Falsy `difficulty_count` (null seen live) → difficulty unset.
- Plain fetch for api.ravelry.com (not guardedFetch); hand-rolled ETag LRU (no new dep).
- Importer 429 = log-only; R3 script waits and retries once.
- R3 owner account via dedicated `RAVELRY_IMPORT_OWNER_*` vars (not `SEED_USER1_*`).
- ravelry.com hard-404s unknown permalinks (checked live) → fall-through case pins 422.

## Open items

- R3 parks until Cece's key arrives: uncomment `RAVELRY_CECE_*` + `RAVELRY_IMPORT_OWNER_*` in
  `.env` → smoke → run ×2 (second run must report 0 created) → she deletes the key. Runbook:
  `docs/ravelry-import-runbook.md`.
- Both R1/R2 gates passed and confirmed by Zara 2026-07-19 (logs in `scripts/.cache/
  verify-importer-r1b.txt` 34/34 and `verify-fresh-r1.txt` ALL GREEN); nothing else open.

## Addendum (same day, after Zara's R1/R2 testing)

- Zara's gates: `verify:fresh` ALL GREEN; `verify:importer` 34/34 after fixing the flaky
  redirect-helper case (httpbin.org 503'd mid-run; the case now falls over to the nghttp2
  mirror, and a helper-down 5xx can't mask a real guard regression).
- `RATE_LIMIT_MAX` 20 → 40 (Zara's call; SPEC §10 + burst test updated) — 20 was sized for
  paste-only flows and verify-runs + browsing in one minute grazed it.
- Rate-limited imports no longer open a near-blank form: 429 is retryable, so the paste door
  shows an inline error and the search screen toasts without navigating; other failures still
  open the form, seeded from the tapped card. `useUrlImport.run` → `'navigated' | 'rate_limited'`.

## Key file paths

`importer/src/ravelry/{types,detect,client,map}.ts` · `importer/src/routes/{extract,ravelrySearch}.ts`
· `importer/src/{app,config}.ts` · `scripts/dev.sh` · `pb/pb_migrations/1784446344_patterns_ravelry.js`
· `web/src/features/patterns/{importerClient,useUrlImport,usePasteLinkDoor,pendingUrlImport,formData}.ts`
· `web/src/features/patterns/components/{PatternForm,QuickAddSheet,RavelrySearchCard}.tsx`
· `web/src/routes/{PatternFormPage,RavelrySearchPage}.tsx` · `web/src/features/home/components/DoorsRow.tsx`
· `web/src/{main.tsx,lib/schema.ts}` · `scripts/ravelry-import.mjs` · `scripts/verify/importer-check.mjs`
· `docs/ravelry-import-runbook.md` · `docs/{DECISIONS,DESIGN}.md` · `plans/p13-ravelry-r1-r2-r3-plan.md`
