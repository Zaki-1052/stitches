# 2026-07-20 · Sessions 6.1–6.3 — yarn stash, offline foundations, vault keep-on-phone

Built as **one continuous session** (Zara's call) from `plans/p14-sessions-6.1-6.3-addons-plan.md`,
per `docs/ADDONS.md` §2–§3 (binding). Web version bumped 0.4.2 → 0.6.3.

## What was done

- **6.1 backend:** `pb_migrations/1784613470_yarn_stash.js` (`yarns` collection + `projects.yarns`
  ≤99 no-cascade + link guard); `rules-check.mjs` extended in place (yarns matrix, 10 link-guard
  fixtures, quiet-unlink cascade proof, sweeps); `seed.mjs` gains 2–3 yarns per user + one starter
  project each, linked to a pattern AND a yarn at create time.
- **6.1 web:** `features/yarn/` (queries/mutations/formData/urlParams + YarnCard/EmptyState/
  FilterSheet/Chip/Form/UsedInList/Picker); routes `/yarn`, `/yarn/new`, `/yarn/:id`,
  `/yarn/:id/edit`; `LibraryTabs` (remembers each root's last search — the independent-filter-state
  criterion); Dock two-root Library active state; project form yarn picker + detail chips;
  `projectKeys.linkedToYarn` reverse query; `schema.ts` YarnRecord + form triple.
- **6.2:** auth boot fix (definitive rejection vs network failure; `unverified` + gated retries);
  `lib/sync.ts` full-library sync (exact runtime queryKeys, detail-cache seeding, journal grouping,
  thumbnail warming, own `requestKey`s); `lib/files.ts thumbUrl()` + 17 call sites refactored;
  offline branch in `normalizePbError`; `useOnlineStatus()` + AppHeader CloudOff; OfflineCard in
  Settings; SW thumbnails 300 → 2000; `PersistQueryClientProvider` persistence (buster, 30 d,
  fileToken excluded).
- **6.3:** `lib/keptFiles.ts` (owner-scoped IDB blobs, identity sweep, sizes); AttachmentsCard
  keep-toggle (one-shot token fetch) + kept-copy cleanup on delete; `useKeptAttachments` object
  URLs; Clear-all in OfflineCard; `KEPT_PDF_VIEW_MODE = 'link'` flip knob.
- **Docs:** SPEC §5/§7/§7.9/§11/§12/§17 + DESIGN §3/§8/§9 updated; 7 DECISIONS lines appended.

## Decisions made

- **Link guard ships own-yarns-only** — ADDONS §2.2's fallback at a proven wall. Gate findings
  (ADDONS §7 #1–4 closed, `logs/verify-fresh-6.1-run3.txt`): bare `yarns = ""` doesn't match an
  empty multi-relation (dot-path `yarns.id = ""` does); bare operators ALL-quantify; the OR of two
  dot-path conditions groups per-aggregate, not per-row (the legit own+shared mix failed both
  arms); `@request.body` multi dot-joins work. Friend-shared yarns viewable, not linkable.
- Used-in query under `projectKeys`; offline UX via the normalizer branch; sync v1 unfiltered-only
  limit; dep pins 5.101.2/5.101.2/6.3.0 — all recorded in DECISIONS 2026-07-20.

## Verification

- `verify:fresh` ALL GREEN ×2 (137 checks/run) after two diagnostic rounds — run 1 caught the
  empty-arm semantics, run 2 caught the per-aggregate OR. Logs: `verify-fresh-6.1{,-run2,-run3}.txt`.
- `npx tsc -b` and `npm run lint` clean post-install (persist-client/idb-keyval `.d.ts` shapes
  verified by the type-check). Interaction flows code-verified by Claude per convention.

## Open items

- Zara's load/look pass (`npm run dev`): `/yarn` tabs + empty state, seeded yarn card/detail,
  project chips, Settings OfflineCard, DevTools-offline header indicator.
- 📱 phase-boundary walk (batched): airplane-mode cold reopen; never-visited pattern offline after
  Sync now; kept-PDF in airplane mode; blob-PDF behavior → `KEPT_PDF_VIEW_MODE` flip decision +
  DECISIONS line; `storage.estimate()` sanity check. `docs/PLAN.md` boxes stay unticked until then.
- Phase 6 remainder: Sessions 6.4 (share links) and 6.5 (clipper), most useful post-Phase 5.

## Key file paths

`pb/pb_migrations/1784613470_yarn_stash.js` · `scripts/{rules-check,seed}.mjs` ·
`web/src/features/yarn/*` · `web/src/lib/{sync,files,keptFiles,network,bytes}.ts` ·
`web/src/lib/auth.tsx` · `web/src/features/shared/{persistence,errors}.ts` ·
`web/src/components/{LibraryTabs,Dock,AppHeader}.tsx` · `web/src/routes/Yarn*.tsx` ·
`web/src/features/settings/components/OfflineCard.tsx` ·
`web/src/features/patterns/{components/AttachmentsCard.tsx,useKeptAttachments.ts}` ·
`plans/p14-sessions-6.1-6.3-addons-plan.md` (archived plan)
