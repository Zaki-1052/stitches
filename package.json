{
  "name": "stitches",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": ["web", "importer"],
  "engines": {
    "node": "24.18.0"
  },
  "scripts": {
    "dev": "bash scripts/dev.sh",
    "build": "npm run build --workspace web",
    "icons": "npm run icons --workspace web",
    "lint": "npm run lint --workspace web",
    "preview": "npm run preview --workspace web",
    "seed": "node --env-file-if-exists=.env scripts/seed.mjs",
    "verify:contrast": "node scripts/verify/contrast.mjs",
    "verify:importer": "node --env-file-if-exists=.env scripts/verify/importer-check.mjs",
    "verify:rules": "node --env-file-if-exists=.env scripts/rules-check.mjs",
    "verify:fresh": "bash scripts/verify-fresh.sh"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "prettier": "3.9.5"
  }
}
